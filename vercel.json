{
  "installCommand": "cd backend && npm install --include=dev && npx prisma generate",
  "buildCommand": "echo static",
  "outputDirectory": ".",
  "functions": {
    "api/*.js": {
      "includeFiles": "backend/**"
    }
  }
}
